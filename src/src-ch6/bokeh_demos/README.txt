
create "solutions" directory by running

python create_solutios_advection_schemes.py

Then run the sliders app from the prompt with:

bokeh serve --show sliders_app_stepfunction_V0.11.1.py

This should open a browser with the app. Otherwise navigate to the following URL in a browser:

http://localhost:5006/sliders_app_stepfunction_V0.11.1